<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qumeric.elections">

    <!-- allowBackup is temporary disabled to suppress warning -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameActivity">

        </activity>
        <activity android:name=".QuestionActivity">

        </activity>
        <activity android:name=".ChangeActivity">

        </activity>

        <activity android:name=".PollActivity">

        </activity>

        <activity android:name=".CatcherGameActivity">

        </activity>

        <activity android:name=".DucksGameActivity">

        </activity>

        <activity android:name=".HammerGameActivity">

        </activity>

        <activity android:name=".LadderGameActivity">

        </activity>

        <activity android:name=".SnakeGameActivity">

        </activity>
    </application>

</manifest>